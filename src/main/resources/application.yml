# Some text
spring:
  profiles:
    active: service
  datasource:
    url: "jdbc:h2:file:./db/tvn;IGNORECASE=TRUE"
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update